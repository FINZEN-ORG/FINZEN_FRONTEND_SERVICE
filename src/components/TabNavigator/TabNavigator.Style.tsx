import { StyleSheet } from 'react-native';

export const tabStyles = StyleSheet.create({
  iconFocused: {
    fontSize: 20,
    opacity: 1,
  },
  iconUnfocused: {
    fontSize: 20,
    opacity: 0.6,
  },
});